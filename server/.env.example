# Required on Vercel for auth (register/login will fail without these):
MONGO_URI=
JWT_SECRET=

EMAIL_USER=
EMAIL_PASS=
# Optional: deployed Python AI service (defaults to http://localhost:8000 locally)
AI_ENGINE_URL=
